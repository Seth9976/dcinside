package androidx.compose.foundation.lazy;

import androidx.compose.foundation.gestures.Orientation;
import y4.l;

public final class b {
    public static int a(LazyListLayoutInfo lazyListLayoutInfo0) [...] // Inlined contents

    public static int b(LazyListLayoutInfo lazyListLayoutInfo0) [...] // Inlined contents

    public static int c(LazyListLayoutInfo lazyListLayoutInfo0) [...] // Inlined contents

    @l
    public static Orientation d(LazyListLayoutInfo lazyListLayoutInfo0) {
        return Orientation.a;
    }

    public static boolean e(LazyListLayoutInfo lazyListLayoutInfo0) [...] // Inlined contents

    // 去混淆评级： 低(20)
    public static long f(LazyListLayoutInfo lazyListLayoutInfo0) {
        return 0L;
    }

    public static int g(LazyListLayoutInfo lazyListLayoutInfo0) {
        return 0;
    }

    public static int h(LazyListLayoutInfo lazyListLayoutInfo0) {
        return 0;
    }

    public static int i(LazyListLayoutInfo lazyListLayoutInfo0) {
        return 0;
    }

    public static boolean k(LazyListLayoutInfo lazyListLayoutInfo0) {
        return false;
    }
}

