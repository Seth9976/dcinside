package com.google.android.material.sidesheet;

import androidx.annotation.RestrictTo.Scope;
import androidx.annotation.RestrictTo;

@RestrictTo({Scope.b})
final class h {
    static boolean a(float f, float f1) {
        return Math.abs(f) > Math.abs(f1);
    }
}

