package com.google.common.graph;

import J1.a;
import O1.f;

@a
@f("Implement with a lambda, or use GraphBuilder to build a Graph with the desired edges")
@x
public interface k0 {
    Iterable a(Object arg1);
}

