package com.unity3d.services.core.connectivity;

public enum ConnectivityEvent {
    CONNECTED,
    DISCONNECTED,
    NETWORK_CHANGE;

    private static ConnectivityEvent[] $values() [...] // Inlined contents
}

