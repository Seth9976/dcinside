package com.unity3d.services.core.request;

public enum ResolveHostError {
    INVALID_HOST,
    UNKNOWN_HOST,
    UNEXPECTED_EXCEPTION,
    TIMEOUT;

    private static ResolveHostError[] $values() [...] // Inlined contents
}

