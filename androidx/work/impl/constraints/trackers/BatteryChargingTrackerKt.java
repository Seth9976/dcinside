package androidx.work.impl.constraints.trackers;

import kotlin.jvm.internal.L;
import y4.l;

public final class BatteryChargingTrackerKt {
    @l
    private static final String a;

    // 去混淆评级： 低(20)
    static {
        L.o("WM-BatteryChrgTracker", "tagWithPrefix(\"BatteryChrgTracker\")");
        BatteryChargingTrackerKt.a = "WM-BatteryChrgTracker";
    }

    // 去混淆评级： 低(20)
    public static final String a() [...] // 潜在的解密器
}

